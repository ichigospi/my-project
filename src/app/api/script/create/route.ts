import { NextRequest, NextResponse } from "next/server";
import { resolveAiModel, anthropicHeaders, anthropicExtraBody } from "@/lib/ai-model";
import { recordUsage } from "@/lib/usage-tracker";

// 6,500〜7,500文字級の台本生成は出力トークン・所要時間ともに大きいため実行上限を延長
export const maxDuration = 300;

interface RefAnalysis {
  videoTitle?: string;
  channelName?: string;
  views?: number;
  analysisResult?: {
    summary?: string;
    structure?: { name: string; timeRange: string; purpose: string }[];
    hooks?: string[];
    ctas?: string[];
    growthFactors?: string[];
    appealPoints?: string[];
    targetEmotion?: string;
    overallPattern?: string;
    idealFuture?: string;
    worstFuture?: string;
    retentionTactics?: string[];
    worldview?: string;
  } | null;
}

// 元ネタ分析を「超えるべき基準値」としてプロンプトに整形する
function buildReferenceText(referenceAnalyses: RefAnalysis[]): string {
  if (!referenceAnalyses || referenceAnalyses.length === 0) return "";

  const blocks = referenceAnalyses.map((a, i) => {
    const r = a.analysisResult;
    if (!r) return "";
    return `■ 元ネタ${i + 1}「${a.videoTitle || "無題"}」（${a.channelName || "不明"} / 再生数: ${a.views?.toLocaleString() || "不明"}回）
・構成: ${r.structure?.map((s) => `${s.name}(${s.timeRange})`).join(" → ") || "不明"}
・理想の未来（欲求喚起）: ${r.idealFuture || r.appealPoints?.join(" / ") || "不明"}
・最悪の未来: ${r.worstFuture || "不明"}
・CTA: ${r.ctas?.join(" / ") || "不明"}
・視聴維持の仕掛け（離脱防止）: ${r.retentionTactics?.join(" / ") || r.hooks?.join(" / ") || "不明"}
・世界観の演出: ${r.worldview || "不明"}
・伸びた要因: ${r.growthFactors?.join(" / ") || "不明"}`;
  }).filter(Boolean);

  if (blocks.length === 0) return "";

  return `
【元ネタ分析（＝あなたが超えるべき基準値）】
${blocks.join("\n\n")}
`;
}

export async function POST(request: NextRequest) {
  const body = await request.json();
  const { proposal, channelProfile, style, topic, additionalNotes, aiApiKey, rulesText, referenceAnalyses } = body;
  const segment: { index: number; total: number; skeletonPart?: string; previousScript?: string; partTargetChars?: number; totalTargetChars?: number } | undefined = body.segment;
  const aiModel = resolveAiModel(body.aiModel);

  if (!aiApiKey) {
    return NextResponse.json({ error: "AI APIキーが設定されていません" }, { status: 400 });
  }

  const isAnthropic = aiApiKey.startsWith("sk-ant-");

  // 骨組みテキスト（マークダウン形式のconceptに入っている）
  const skeletonText = proposal?.concept || "";

  const referenceText = buildReferenceText(referenceAnalyses);

  const profileText = channelProfile ? `
【自チャンネル設計】
チャンネル名: ${channelProfile.channelName || "未設定"}
コンセプト: ${channelProfile.concept || "未設定"}
口調・話し方: ${channelProfile.tone || "未設定"}
ターゲット層: ${channelProfile.target || "未設定"}
得意ジャンル: ${channelProfile.genres?.join(", ") || "未設定"}
スタイル: ${channelProfile.mainStyle === "healing" ? "ヒーリング系メイン" : channelProfile.mainStyle === "education" ? "教育系メイン" : "両方"}
特徴・こだわり: ${channelProfile.characteristics || "未設定"}
` : "";

  const prompt = `あなたはプロのスピーチマーケター兼YouTube台本ライターです。
あなたの仕事は、元ネタの台本を分析した上で、それを確実に超える「上位互換」の台本を書くことです。

【最優先のルール優先順位】
矛盾が生じた場合は必ずこの順で優先すること。
  1位: チャンネル共通ルール（${rulesText ? "下記【チャンネル共通ルール】" : "指定があれば"}）の必須要素・書式指定
  2位: タイトルとの整合性（タイトルの約束を本文で必ず回収する）
  3位: 元ネタの構成トレース
- 元ネタは競合動画なので、このチャンネル独自の必須要素（公式LINE誘導・収益化導線・放置危険性の警告など）は含まれていない。
  元ネタや骨組みに無くても、チャンネル共通ルールで必須とされた要素は専用ブロックとして必ず台本に追加すること。これは構成トレースより優先する。
- 特に「公式LINE誘導」「放置するとどうなるかの警告」は、ルールで要求されていれば絶対に省略しない。CTAパートに収益化導線が無い台本は失格。

【最重要ミッション：元ネタの上位互換を作る】
以下の3原則を満たしてください。

原則1. 元ネタの構成をトレースする
- 元ネタ分析の構成（セクションの順番・役割・尺配分）を基本的に踏襲する
- 勝手に構成を組み替えたり省略したりしない
- ただし上記【ルール優先順位】の通り、チャンネル必須要素は元ネタに無くても追加する

原則2. 元ネタの5要素を、より具体的・よりターゲットに刺さる内容に上書きする
元ネタ分析に書かれた以下5要素は「超えるべき基準値」です。同等以下は失格。各要素で元ネタより一段具体的にすること。
  (1) 理想の未来（欲求喚起）… 元ネタより具体的な数字・固有のシチュエーション・五感描写で、視聴者の欲求を強く喚起する
  (2) 最悪の未来 … 元ネタより具体的に「放置するとどうなるか」を描き、回避欲求を刺激する
  (3) CTA … 元ネタより自然かつ行動の理由が明確で、視聴者が動きたくなる導線にする
  (4) 視聴維持率の確保（離脱防止）… 元ネタの仕掛けに加え、各セクション冒頭で続きを見たくなる予告・問いかけ・感情の起伏を入れる
  (5) 世界観の演出 … 元ネタより没入感のある語り口・比喩・独自用語で、チャンネルの世界観を一貫させる
※元ネタ固有の固有名詞・キャラクター・エピソードはコピーせず、自チャンネル向けに具体性のレベルだけを引き継ぐこと。
※元ネタ分析（下記【元ネタ分析】）は、骨組みより優先する「具体性のソース」。骨組みが抽象的でも、元ネタの金額・シチュエーションのレベルまで必ず具体化すること。
※【フレーズ単位のパクリ厳禁（必達）】元ネタ分析・元ネタ台本に書かれた以下を**そのまま流用するのは禁止**:
  - 具体的な金額・宝くじ種類・時期・売り場の特徴（例: 「翌月にジャンボで1等5億円」「言われた日に言われた売り場」）
  - 3つの疑問形の中身（例: 「いつ買えばいいか／どこで買えばいいか／いくら当たるか」）
  - 比喩・対比表現（例: 「天と地ほどの差」「水と油」）
  - 体験談のセリフ・ナレーション・言い回し
  - 固有のフック文・締め文・キャッチフレーズ
  引き継ぐのは **構造・役割・具体性のレベル** だけ。語句・固有要素・数値設定は全て自チャンネル独自に作り直すこと。
  ・良い例: 元ネタ「翌月にジャンボで1等5億円」→ 自前「3週間後にミニロトで780万円」（構造維持・具体性レベル維持・語句変更）
  ・良い例: 元ネタ「いつ／どこで／いくら」→ 自前「どの宝くじを選ぶか／直前の所作／当選後の使い道」（質問内容そのものを変更）
  ・良い例: 元ネタ「天と地ほどの差」→ 自前「朝日と夕暮れほどの差」「月とすっぽんほどの差」
  ・悪い例: 元ネタの数値・宝くじ種類・売り場特徴・疑問形の中身・比喩をそのまま使う → 一致率25%以上で失格

原則3. マーケティング上、無駄な文章を1文も入れない
- どの一文も「欲求喚起・離脱防止・信頼構築・行動喚起」のいずれかの役割を持つこと
- 役割のない前置き・繰り返し・冗長な言い回し・当たり障りのない一般論は書かない
- 文字数を埋めるための水増しは禁止。密度を最優先する

原則4. 内部整合性（矛盾・重複の禁止）
- 台本全体で主張を1つに統一する。あるパートと別のパートで食い違う説明を絶対にしない
  - 悪い例: 前半で「高評価が6億を受け取るサインです」と言い、後半で「コメントに『6億受け取ります』と書くのが受け取りのサインです」と言う（"受け取るサイン"が2つあって矛盾）
  - 「○○を受け取るサイン／条件／方法」は台本を通して1つだけに決め、最後までブレさせない。各CTAには別々の意味を持たせる（例: 高評価＝感謝の循環、コメント＝願いの宣言、LINE＝エネルギーの固定、のように役割を分ける）
- 同じCTAを何度も繰り返さない。高評価・コメント・チャンネル登録・LINE登録はそれぞれ1回ずつ、役割を分けて配置する
  - 特にチャンネル登録を複数回要求しない（くどくなる）。CTAごとに「なぜそれをするのか」の理由を1つに絞る
- **CTA間の理由づけが矛盾しない（最重要）**
  - 各CTAが「解決する課題」を必ず別物にする。前のCTAで解決した課題を、後のCTAでまた未解決として持ち出すのは禁止
  - 悪い例: 序盤で「エネルギーを固定するためにコメントで『受け取ります』と書いて」と誘導しておきながら、終盤のLINE誘導で「エネルギーが定まらないので守護画像を受け取って」と言う。視聴者が「コメントしたのに無駄だったの？」と感じる重大NG
  - 良い例: コメント＝願いの宣言（意思表示）、守護画像＝既に入ったエネルギーの固定、霊視鑑定＝具体的な行動指針の取得 のように、各CTAの担当領域を明確に分けて、前のCTAの効果を後ろのCTAで否定しないこと

【体験談・社会的証明の書き方（必達）】
- 体験談・お客様の声・成功事例を「」で囲んで提示する場合、**必ずお客様本人の一人称セリフ（直接話法）で統一する**
- ナレーターが第三者として説明する「○○という方もいらっしゃいます」「○○されたご報告があります」型の又聞き口調を「」の中に入れるのは絶対NG
  - 悪い例（混在で破綻）:
    「再生した翌日に、大きな臨時収入の通知が届きました」（←本人の声で正しい）
    「宝くじで1等2億円が当選したという方もいらっしゃいます」（←ナレーター視点で「」の中に入ってる＝NG）
    「遺産と保険が重なり合計9000万円以上の臨時収入があったという方もいらっしゃいます」（←同上NG）
  - 良い例（一人称セリフで統一）:
    「再生した翌日に、3000万円の臨時収入の通知が届きました」
    「半年諦めかけてた宝くじを買い直したら、1等で2億円当たりました」
    「亡くなった父の遺産と保険が重なって、合計9000万円が一気に入ってきました」
- 体験談は必ず本人の口調（〜しました／〜でした／〜です／〜入ってきました）で書く
- ナレーターが第三者紹介として地の文で書く場合は「」で囲まない（「先月、ロト6で1億円当てた方からご報告が届いてます」のように地の文として扱う）
- 「」で囲んだら本人の声、地の文ならナレーター視点、を絶対に混在させない

【タイトル整合性（必達）】
タイトル「${topic}」に含まれる数字・スケール・約束（例:「億」「3分後」「100万人に一人」等）は、
本文のフック・体験談・理想の未来の中で、必ず同じレベルの具体性で回収すること。
タイトルが「億」を謳うのに本文の事例が数万円台、のような乖離は重大NG。

【テーマ／タイトル】${topic}
【スタイル】${style === "healing" ? "ヒーリング系（癒し・瞑想・エネルギーワーク中心）" : style === "tarot" ? "タロット系（リーディング進行型。1対1で視聴者に語りかけながら、カードを4〜5枚順番に引いて、その都度カードの意味を読み解いてメッセージを伝える）" : "解説・教育系（知識・解説中心）"}
${profileText}
${referenceText}${style === "tarot" ? `
【タロットスタイル時の最優先指示（必達）】
- これは「タロットリーディング」の動画。カードを実際に4〜5枚、順番に引きながら結果を伝える構成にすること。
- 各カードごとに「カード名 → そのカードの意味 → 視聴者の状況への紐付け」を必ず行う。
- チャンネル共通ルールや骨組みに【中盤】ヒーリング音楽パート等のヒーリング構成・瞑想/呼吸誘導/アファメーション連打が書かれていても、タロットスタイルでは適用しない。それらに引っ張られてヒーリング台本にしてはいけない。
- 元ネタ(参考動画)がカードを引きながらリーディングしているなら、その「カードを順に引いて読み解く」進行を必ずトレースする。
- 山選択(A/B/C)は使わない。最初から最後まで1人の視聴者に向けた単一のリーディングとして進める。
` : ""}
【総文字数（必達）】
- 台本全体は【追加指示】で指定された目標文字数（およそ4,000〜5,000文字）に収めること。
- 骨組みや他の箇所に「目標6,500〜7,500文字」等の大きい数字が書かれていても無視し、この目標を優先する。
- 過度な肉付け・水増し・同じ主張の繰り返しで文字数を膨らませない。密度を優先し、役割の無い文は書かない。

【台本の骨組み（この構成に沿って台本を書くこと）】
${skeletonText}

${rulesText || ""}
${additionalNotes ? `【追加指示】\n${additionalNotes}` : ""}

【書式ルール（チャンネル共通ルールに書式指定がある場合は必ずそちらを最優先）】
- チャンネル共通ルールに「記号（##・---・*等）を使わない」「1行○文字で改行」「【前半】【中盤】【終盤】等のラベルを付ける」といった書式指定があれば、それを厳守する
- 書式指定が無い場合のみ、各セクションを「## セクション名」で区切る
- 同じ語尾の連続使用の制限がルールにある場合でも、ヒーリングパート・アファメーションパートには適用しない（そこは繰り返しが演出として有効なため、語尾が連続してよい）。それ以外のパートでは制限を守る

以下のルールに従って台本を書いてください：
- 視聴者に直接語りかける温かく親しみやすい口調
- ${style === "healing" ? "癒しと安心感を与える穏やかなトーン" : style === "tarot" ? "カードを1枚ずつ引きながら読み解く、神秘的で温かいリーディングのトーン" : "わかりやすく知識を伝える信頼感のあるトーン"}
- セリフ形式で書く（「」は使わず、そのまま話す言葉として書く）
- 抽象論ではなく、具体的なエピソード・数字・例え話で語る
- 感情に訴えるフレーズを要所に入れる
- 台本テキストのみを出力（メタ的な説明・分析コメントは不要）

【プロのスピーチライターレベルの文章作法（必達）】
素人っぽい単調な文章は禁止。読み上げた瞬間に「うまい」と感じさせるリズムと論理運びで書くこと。

1. 語尾のバリエーションを徹底する（ヒーリング/アファメーションパートは除く）
   - 「〜です。〜です。〜ます。」のように同じ語尾を3回以上続けるのは禁止
   - 「〜です／〜ます／〜なんです／〜ですよね／〜でしょう／〜のです／〜と思いませんか／体言止め」など複数の語尾を交互に混ぜる
   - 悪い例:「これは大切です。あなたに必要です。覚えておくことです。」
   - 良い例:「これは大切なこと。なぜならあなた自身の人生を変える力を持っているからです。覚えておいてくださいね。」

2. 接続詞・繋ぎ表現で論理の流れを作る
   - 文を単純に並べず、「つまり〜ということなんです」「なぜなら〜だからです」「ところが実は〜」「だからこそ〜」「その結果〜」「言い換えると〜」などで因果・対比・転換・言い換えを明示する
   - 視聴者の頭の中で「なるほど→だから→つまり」と論理が積み上がる流れにする
   - 悪い例:「お金が入ります。豊かになります。幸せになります。」
   - 良い例:「お金が入ってきます。つまり、ただ生活が楽になるだけじゃなく、心に余裕が生まれて、本当の意味で豊かな日々が始まるということなんです。」

3. 短文と長文のリズムを混ぜる
   - 重要な主張の前後には短文を置いて間を作る（「これ、本当です。」のように）
   - 説明・描写は適度な長さで展開し、緩急をつける
   - 全文が同じ長さだと聞き手が眠くなる

4. 含蓄のある言い回しを使う
   - 一文の中に「主張＋根拠／主張＋言い換え／問いかけ＋答え」の構造を入れる
   - 視聴者の心の声を代弁する一言を要所に入れる（「気づいてましたか？」「実はこれ、多くの人が見落としているんです」）
   - 比喩・対比・数字を使って具体性と説得力を両立させる

5. 単調な並列を避ける
   - 「Aです。Bです。Cです。」のような並列の羅列は、「Aです。そしてBも。さらに言えば、Cまで起こり始めるんです」のように接続でつなぐ

【終盤クロージングの書き方（全動画共通・必達）】
※元ネタ分析や骨組みに別形式のCTAが書かれていても、終盤クロージングは必ずこのパターンで書く。CTAに関する指示が重複する場合はこちらを優先する。

A. 視聴完了の意味付け
   - 「最後まで見届けることができましたね」のように視聴行為を儀式化
   - 既成事実化の断定形を使う（「もう○○は決まっています」）

B. 軽CTAの3例文
   - チャンネルの軽CTA（多くはコメント誘導）には、視聴者が真似しやすい例文を必ず3つ並べる
   - 例文は短く、それぞれ違う動機を反映させる（家族／自分／他者 など）

C. 軽CTA直後の社会的証明
   - 「以前○○と書いた方が、翌月××になりました」型で具体時系列＋具体金額/結果を1件
   - 「多くの方が」のような抽象表現は禁止
   - お客様の声を「」で引用する場合は必ず本人の一人称セリフで書く。「○○という方もいらっしゃいます」型のナレーター視点を「」に入れるのは絶対NG

D. 軽→重の橋渡し（サンクコスト型・損失回避×インセンティブの連結）★最重要
   - 「損失回避 → 具体的なアクション → そのアクションが何を達成するか」を必ずワンセットで書く。3点が繋がっていないのは失格
     例:「このまま放置すると、せっかく整った流れが元の状態に戻ってしまいます（損失回避）。だからこそ、概要欄のLINEから白蛇さまの守護画像を受け取ってください（アクション）。それがあなたの流れを固定し、エネルギーが外へ逃げないようとどめてくれます（達成される結果）」
   - 「損失だけ語って終わる」「アクションだけ促して終わる」は禁止。必ず損失→アクション→結果の3点を一続きで繋げる
   - 重CTAを「これまでの状態を守るための延長行動」として位置づける

E. 重CTAの価値再定義
   - 「もう一つ、一番大事なことを伝えます」型の再フックで始める
   - 「○○は決まっていますが、それだけだと『いつ』『どこで』『いくら』が分かりません」のように、軽CTAでは埋まらない3つの疑問形で不完全感を演出
   - 「一人ひとり全く違う」と量産感を否定

F. 重CTA直後の社会的証明（スケールアップ）
   - 軽CTA直後の事例より明確に大きいスケール（金額・成果）の事例を1〜2件
   - 段階的に大きくして「重CTAの先はもっと凄い」と印象づける
   - お客様の声を「」で引用する場合は必ず本人の一人称セリフで書く。「○○という方もいらっしゃいます」型のナレーター視点を「」に入れるのは絶対NG

G. 比較強調
   - 「受けた人と受けなかった人では天と地ほどの差」型の明示的な対比を1文

H. クロージング
   - 期限的限定（「いつまで続くか分かりません」）
   - 即時性（「今のうちに」「○○が強いうちに」）
   - 行動の最後の一押し（「必ず受け取ってください」）

※「○○」の中身はチャンネルの世界観・テーマに合わせる（龍神／守護天使／金運／恋愛 等）
※具体的な軽CTA／重CTAの内容はチャンネル共通ルールに従う

【守護画像→霊視鑑定の一本線テンプレ（重CTAで両方を出す場合の必達）】
重CTAでLINE登録の対価として「守護画像」と「霊視鑑定」の両方を提示する場合、
理由づけを必ず下記の1本の線として接続すること。各ブロックの順番・役割を入れ替えない。

  ① ベネフィット獲得宣言:「○○のエネルギー／流れは、もうあなたに入ってきています」（既成事実化）
  ② 損失リスク提示:「ただ、このまま放置するとあなたの波動は元の状態に戻ろうとします」（時間経過による損失）
  ③ 守護画像CTA:「だからこそ、エネルギーを"固定"するために白蛇の守護画像をスマホに保存してください」
     ※守護画像の役割=「入ったエネルギーを定着・固定する守りの装置」
  ④ 不完全感の提示:「ただし、今のままでは"具体的にいつ・どうやれば○○を引き寄せられるか"までは視えない状態です」
     ※守護画像で解決した課題(固定)と別の新しい課題(具体的行動指針)を提示
  ⑤ 霊視鑑定CTA:「今なら無料の霊視鑑定が受けられます」
     「○○を引き寄せるために、いつ・何をすればいいかを白蛇様を通してお伝えします」
     ※霊視鑑定の役割=「具体的な行動指針を授ける攻めの装置」
  ⑥ 比較強調:「これを受けた人と受けなかった人では、こういう差が生まれています」（軽CTA直後より大きいスケールの事例）

【絶対NG】
- ③で守護画像により"エネルギーが固定された"と言いながら、⑤の霊視鑑定の理由で再び「エネルギーが定まらない」と言うこと
  → ③と⑤は解決する課題が違う（固定 vs 行動指針）と明確に区別する
- ④の不完全感を③の守護画像と同じ課題（=エネルギーの定着）にしないこと
  → 守護画像が解決するのは「定着」、霊視鑑定が解決するのは「具体行動」。役割を絶対に被らせない
- ①〜⑥の順番を入れ替えない。1本の線で繋がる理由づけにする

【重要・自チャンネルpersona】
- ナレーターの名乗り（自己紹介・自分の名前）は **${channelProfile?.channelName ? `必ず "${channelProfile.channelName}" を使う` : "入れない（未設定のため）。代わりに「あなた」視点の二人称語りで進行する"}**
- 参考動画/骨組みに登場する別の人物名（例: アリサ, エリサ, かぐら 等）が出てきても **絶対にそのまま使わない**
- 参考動画固有のキャラクターストーリー（特定人物との対話シーン等）はチャンネル独自のキャラ・対象者語りに置き換える`;

  // 分割出力モード：台本を total 回に分けて生成する際の、今回パート専用の追加指示
  let segmentDirective = "";
  if (segment && segment.total > 1) {
    const { index, total, skeletonPart, previousScript } = segment;
    const isFirst = index === 0;
    const isLast = index === total - 1;
    // 総目標文字数はフロントから受け取る（プリセットの目標文字数）。無ければ控えめに5200。
    const totalTarget = segment.totalTargetChars && segment.totalTargetChars > 0 ? segment.totalTargetChars : 5200;
    // このパートの割当（骨組みの分量比で重み付けした値をフロントから受け取る。無ければ均等割り）
    const perPart = segment.partTargetChars && segment.partTargetChars > 0 ? segment.partTargetChars : Math.round(totalTarget / total);
    const perPartCap = Math.round(perPart * 1.1);
    segmentDirective = `

=============================
【分割出力モード（最優先・必達）】
- この台本は全${total}回に分けて出力します。今回は第${index + 1}回目（${index + 1}/${total}）です。
- 下記【今回書く範囲】に該当する骨組みセクションだけを台本化してください。範囲外のセクションは絶対に書かないこと。
- ${isFirst ? "これは台本の最初のパートです。冒頭の選民フック（おめでとう/選ばれた等）から書き始めること。" : "これは続きのパートです。冒頭の挨拶・自己紹介・選民フックの再導入はしない（すでに前のパートで済んでいる）。前回の最後の流れから自然に続く形で書き始めること。"}
- ${isLast ? "これは最終パートです。終盤クロージング・無料鑑定への重CTA・締めの挨拶まで完全に書ききること。" : "これは途中のパートです。動画を締めくくるクロージング・最後の重CTA・締めの挨拶はまだ書かない（最終パートで書く）。今回の範囲の内容で自然に区切ること。"}
- 前のパートと同じ文・同じ内容を繰り返さない。トーン・世界観・人物像・語り口は前のパートと完全に一致させること。
- 台本本文のみを出力。「第${index + 1}パート」などの見出しやメタ的な注記は書かない。
${!isFirst && previousScript ? `
【繋ぎ目を自然にする（必達）】
- 下部の【直前の締め】は、前のパートの本当に最後の数文です。今回の最初の一文は、この続きとして途切れず自然に繋がるように書き始めること。
- 具体的には：新しい挨拶・呼びかけで仕切り直さない／前のパートで既に話した話題を要約・再説明しない／「さて」「それでは」等で露骨に場面転換しない（前の文からそのまま話が流れるように）。
- 前のパートが問いかけや予告（オープンループ）で終わっていれば、今回はその答え・回収から入る。
- 代名詞・接続（「その」「だからこそ」「そうやって」等）で前の内容を受けて、地続きの一つの語りとして繋げること。` : ""}

【文字数の割当（超過厳禁・最優先）】
- 台本全体は約${totalTarget}文字に収めます。このパートの割当は「約${perPart}文字」、上限は「${perPartCap}文字」です。
- ${perPartCap}文字を絶対に超えないこと。超えそうな場合は、水増し・冗長な言い換え・同じ主張の繰り返しを削って割当内に収める。
- 骨組みや他の指示に「目標6,500〜7,500文字」等の総文字数が書かれていても、それは無視し、この割当（約${perPart}文字／上限${perPartCap}文字）を最優先する。
- 【過度な肉付けの禁止】1つの要素を長々と説明し直さない。骨組みに書かれた各セクションを、割当文字数に収まる密度で1回ずつ表現する。分割しているからといってパートを膨らませない（各パートの合計が総目標を超える原因になる）。
- どの一文も役割（欲求喚起／離脱防止／信頼構築／行動喚起）を持たせ、役割の無い装飾文・前置き・一般論は書かない。
${skeletonPart ? `\n【今回書く範囲（骨組み抜粋）】\n${skeletonPart}` : ""}
${previousScript ? `\n【前のパートまでに生成済みの台本（全文の文脈。これを繰り返さず、ここから自然に続けること）】\n${previousScript}` : ""}
${!isFirst && previousScript ? `\n【直前の締め（前パートの最後の数文。今回の最初の一文はこの直後に途切れず続ける）】\n…${previousScript.slice(-200)}` : ""}
=============================`;
  }

  const fullPrompt = prompt + segmentDirective;

  try {
    let text = "";

    if (isAnthropic) {
      let res: Response | null = null;
      for (let attempt = 0; attempt < 3; attempt++) {
        res = await fetch("https://api.anthropic.com/v1/messages", {
          method: "POST",
          headers: anthropicHeaders(aiApiKey, aiModel),
          body: JSON.stringify({
            model: aiModel,
            max_tokens: 16000,
            messages: [{ role: "user", content: fullPrompt }],
            ...anthropicExtraBody(aiModel),
          }),
        });
        if (res.status === 429 || res.status === 529) {
          if (attempt === 2) return NextResponse.json({ error: "Overloaded", retryable: true }, { status: res.status });
          await new Promise((r) => setTimeout(r, 5000 * (attempt + 1)));
          continue;
        }
        break;
      }
      if (!res!.ok) {
        const error = await res!.json();
        return NextResponse.json({ error: error.error?.message || "API error" }, { status: res!.status });
      }
      const data = await res!.json();
      recordUsage({ model: data.model || aiModel, usage: data.usage });
      text = (data.content || []).filter((b: { type?: string }) => b.type === "text").map((b: { text?: string }) => b.text || "").join("");
      if (!text && data.stop_reason === "refusal") {
        return NextResponse.json({ error: "安全システムにより生成が拒否されました。表現を調整して再試行するか、モデルをOpus/Sonnetに切り替えてください。" }, { status: 502 });
      }
    } else {
      const res = await fetch("https://api.openai.com/v1/chat/completions", {
        method: "POST",
        headers: { "Content-Type": "application/json", Authorization: `Bearer ${aiApiKey}` },
        body: JSON.stringify({
          model: "gpt-4o",
          messages: [{ role: "user", content: fullPrompt }],
          max_tokens: 16000,
        }),
      });
      if (!res.ok) {
        const error = await res.json();
        return NextResponse.json({ error: error.error?.message || "API error" }, { status: res.status });
      }
      const data = await res.json();
      recordUsage({ model: "gpt-4o", usage: data.usage });
      text = data.choices?.[0]?.message?.content || "";
    }

    return NextResponse.json({ script: text });
  } catch {
    return NextResponse.json({ error: "台本生成に失敗しました" }, { status: 500 });
  }
}
